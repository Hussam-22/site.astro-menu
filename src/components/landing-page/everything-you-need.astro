---
import FeatureCard from '@components/ui/feature-card.astro'

const features = [
	{
		title: 'cloud POS, kitchen and admin dashboard',
		description: 'No physical lock-in, everything runs on the cloud !!',
		bgColor: 'bg-cyan-400',
		imgSrc: '/assets/icons/cloud.svg'
	},
	{
		title: 'Multi-Languages and Translation',
		description: `No more language barrier, don't miss out on any customer`,
		bgColor: 'bg-green-400',
		imgSrc: '/assets/icons/translate.svg'
	},
	{
		title: 'Self Order',
		description: `Customers dont have to wait for a waiter to take their order`,
		bgColor: 'bg-purple-400',
		imgSrc: '/assets/icons/self-order.svg'
	},
	{
		title: 'And many more features',
		description: `Check out our Docs page for more features`,
		bgColor: 'bg-white',
		imgSrc: '/assets/icons/features.svg'
	}
]
---

<section class="mx-auto rounded-lg">
	<div>
		<h2 class="text-center text-4xl font-bold text-white lg:text-5xl lg:tracking-tight">
			Astro-Menu comes batteries included
		</h2>
		<p class="mt-4 text-center text-lg text-white">
			Astro-Menu is packed with powerful features such as Cloud POS/Dashboard, Kitchen Dashboard,
			Auto Translation, and Self-Order capabilities, ensuring your food business runs seamlessly.
			Don’t settle for less when you can have it all.
		</p>
	</div>

	<div class="mt-16 grid grid-cols-1 gap-6 sm:grid-cols-2">
		<div class="grid-row-2 grid gap-6">
			<FeatureCard
				className={features[0].bgColor}
				title={features[0].title}
				description={features[0].description}
				bgSVG={''}
				imgSrc={features[0].imgSrc}
			/>
			<FeatureCard
				className={[features[3].bgColor, 'hidden flex-col sm:flex']}
				title={features[3].title}
				description={features[3].description}
				bgSVG={''}
				btnText="Docs Page"
				btnLink="/docs/welcome/introduction"
				imgSrc={features[3].imgSrc}
			/>
		</div>
		<div class="flex flex-col gap-8">
			{
				features
					.slice(1, 3)
					.map((feature) => (
						<FeatureCard
							className={feature.bgColor}
							title={feature.title}
							description={feature.description}
							bgSVG={''}
							imgSrc={feature.imgSrc}
						/>
					))
			}
		</div>
		<FeatureCard
			className={[features[3].bgColor, 'sm:hidden']}
			title={features[3].title}
			description={features[3].description}
			bgSVG={''}
			btnText="Docs Page"
			btnLink="/docs/welcome/introduction"
			imgSrc={features[3].imgSrc}
		/>
	</div>
</section>
