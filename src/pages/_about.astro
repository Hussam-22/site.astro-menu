---
import { getCollection } from 'astro:content'
import { Picture } from 'astro:assets'
import Layout from '@layouts/Layout.astro'
import Container from '@components/container.astro'
import Sectionhead from '@components/sectionhead.astro'

// Filter team entries with 'draft: false' & date before current date
const publishedTeamMembers = await getCollection('team', ({ data }) => {
	return !data.draft && data.publishDate < new Date()
})
---

<Layout title="About">
	<Container>
		<Sectionhead>
			<Fragment slot="title">About</Fragment>
			<Fragment slot="desc">How everything started !!</Fragment>
		</Sectionhead>

		<!-- <div class="mx-auto mt-16 flex max-w-4xl flex-col gap-3">
			<p class="text-lg leading-relaxed text-white">
				Astro-Menu was born in 2019 during a family vacation to Bulgaria. Hussam and his family
				encountered challenges when trying to navigate restaurant menus. The first issue was the
				language barrier — although menus were available in English, the translations were often
				inaccurate. The second challenge was the lack of dish images, making it difficult to choose
				the right meal. These experiences sparked the idea for Astro-Menu.<br />
				<br />
				While many QR menu solutions exist, Astro-Menu stands out by combining a sleek, modern design
				with cutting-edge technology. It’s built to be intuitive, ensuring a seamless experience from
				the very first scan, with virtually no learning curve required. <br />
				<br />
				Unlike other platforms, Astro-Menu empowers food business owners by offering monthly subscriptions
				with no hidden fees and no revenue cuts. You retain full control over how you handle payments—whether
				it’s cash, credit cards, or any other method—while we take care of the menu.
			</p>
		</div>
		<div class="mx-auto mt-12 grid max-w-4xl gap-10 md:grid-cols-3">
			{
				publishedTeamMembers.map((teamMemberEntry) => (
					<div class="group">
						<div class="aspect-square w-full">
							<Picture
								src={teamMemberEntry.data.avatar.src}
								alt={teamMemberEntry.data.avatar.alt}
								sizes="(max-width: 800px) 100vw, 400px"
								width={400}
								height={400}
								class="aspect-square w-full rounded rounded-md bg-white object-cover object-center transition group-hover:-translate-y-1 group-hover:shadow-xl"
							/>
						</div>

						<div class="mt-4 text-center">
							<h2 class="text-lg">{teamMemberEntry.data.name}</h2>
							<h3 class="text-sm">{teamMemberEntry.data.title}</h3>
						</div>
					</div>
				))
			}
		</div> -->
	</Container>
</Layout>
